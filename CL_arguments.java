//implementing the use of command line arguments

public class CL_arguments {
	public static void main(String args[]) {
		/*the main method has a default array of string as an argument.
      we can use it to to take command line arguments.
      When you run the program manually, add your arguments with it.
    */
		
		System.out.println("Your first line in argument is: " + args[0]);          //args[0] is the first argument of command line
		//the above statement will work in JDK using the system's terminal/console
    
		
	}
}
